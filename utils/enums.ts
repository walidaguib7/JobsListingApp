export enum Role {
  Recruiter = 'Recruiter',
  Admin = 'Admin',
  User = 'User',
}

export enum JobType {
  Part_time = 'Part_time',
  Full_time = 'Full_time',
  Internship = 'Internship',
}

export enum ApplicationStatus {
  pending = 'pending',
  processing = 'processing',
  submited = 'submited',
}
